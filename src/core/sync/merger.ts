import type { Pattern } from "../../types/index.js";

/**
 * パターンからCLAUDE.mdのセクションを生成する
 */
export function generatePatternsSection(_patterns: Pattern[]): string {
  // TODO: 実装 - テンプレートを使用してセクションを生成
  return "";
}

/**
 * 既存のパターンセクションとマージする
 */
export function mergePatternsSections(
  _existing: string | null,
  _newSection: string
): string {
  // TODO: 実装
  return "";
}
