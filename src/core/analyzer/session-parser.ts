import type { SessionEntry } from "../../types/index.js";

/**
 * JSONLセッションログをパースする
 */
export async function parseSessionLog(
  _filePath: string
): Promise<SessionEntry[]> {
  // TODO: 実装
  return [];
}

/**
 * セッションログディレクトリからセッションIDを取得
 */
export async function listSessions(_projectPath?: string): Promise<string[]> {
  // TODO: 実装
  return [];
}
